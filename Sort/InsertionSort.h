#pragma once
class InsertionSort
{
public:
	/// <summary>
	/// 삽입정렬
	/// </summary>
	/// <param name="array">정렬할 배열</param>
	/// <param name="size">배열 사이즈</param>
	/// <param name="showOption">정렬과정 보기</param>
	static void sort(int* array, int size, bool showOption);
};